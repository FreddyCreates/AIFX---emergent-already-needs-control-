# Feedback: Dismiss false positive HALT_ON_REWARD_DIVERGENCE escalation
# The governance report (docs/governance-report.md) shows no escalations this
# cycle (risk score 0.1706). The escalation is a false positive caused by
# missing learning telemetry (reward_signal undefined) being treated as NaN.

id: "fb-2026-06-01-001"
actor: "human://operator"

target:
  type: "decision"
  entity: "atlas://bot/organism-learning-bot"
  law_id: "governance://law/learning-stability"
  rule_name: "HALT_ON_REWARD_DIVERGENCE"
  pipeline_id: "pipeline://governance/learning_cycle"
  run_id: "atlas-governance-cycle-2026-06-01"

decision:
  system: "ESCALATE"
  human: "DISMISS"

context_snapshot:
  health_dashboard: "green"
  risk_score: 0.1706
  findings:
    - "no_reward_divergence_detected"
    - "reward_signal_missing"

rationale: "This escalation is a false positive: the learning cycle evaluated reward divergence with a missing reward signal (reward_signal undefined). In JavaScript, isNaN(undefined) is true, so the rule/branch triggers even though there is no evidence of reward divergence. No manual inspection of training data is required for this cycle."

law_proposal:
  type: "CONDITION_TIGHTENING"
  suggestion: "Require reward_signal to be present (not null/undefined) before evaluating HALT_ON_REWARD_DIVERGENCE, to avoid isNaN(undefined) false positives."

at: "2026-06-01T22:36:00Z"
