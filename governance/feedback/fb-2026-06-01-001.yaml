# Feedback: Dismiss false positive HALT_ON_REWARD_DIVERGENCE escalation
# The governance report (docs/governance-report.md) shows no escalations this
# cycle, risk score 0.1706, and the rule HALT_ON_REWARD_DIVERGENCE does not
# exist in bot-fleet.cpl-l. This escalation is incorrect.

id: "fb-2026-06-01-001"
actor: "human://operator"

target:
  type: "decision"
  entity: "atlas://bot/organism-learning-bot"
  law_id: "governance://law/BOT_FLEET_SAFETY"
  rule_name: "HALT_ON_REWARD_DIVERGENCE"
  pipeline_id: "pipeline://governance/bot_cycle"
  run_id: "governance-cycle-2026-06-01"

decision:
  system: "ESCALATE"
  human: "DISMISS"

context_snapshot:
  health_dashboard: "green"
  risk_score: 0.1706
  findings:
    - "no_reward_divergence_detected"
    - "rule_not_defined_in_cpl_l"

rationale: "The governance report shows ALL CLEAR with no escalations this cycle and a risk score of 0.1706 (well below the 0.7 escalation threshold). The rule HALT_ON_REWARD_DIVERGENCE does not exist in the bot-fleet.cpl-l law file — the learning bot only defines PREVENT_WEIGHT_DIVERGENCE. This escalation is a false positive and no manual inspection of training data is required."

law_proposal:
  type: "RULE_ADDITION"
  suggestion: "Add HALT_ON_REWARD_DIVERGENCE rule to organism-learning-bot in bot-fleet.cpl-l so that future governance cycles can properly evaluate reward divergence conditions instead of generating spurious escalations."

at: "2026-06-01T22:36:00Z"
